Some examples for running different types of atmospheres can be found here, including the ones shown in Tsai+ 2021.

These are formatted in the legacy structure. Before being used, they should be adapted into the object-oriented structure used in `config.py`.
